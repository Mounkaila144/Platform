{% extends 'base.html.twig' %}

{% block title %}Branche{% endblock %}

{% block body %}
    <h1>Branche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ branche.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ branche.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_branche_index') }}">back to list</a>

    <a href="{{ path('app_branche_edit', {'id': branche.id}) }}">edit</a>

    {{ include('branche/_delete_form.html.twig') }}
{% endblock %}
